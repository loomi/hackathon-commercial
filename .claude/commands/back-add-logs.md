---
description: Add structured NestJS Logger calls at the right places and levels
argument-hint: [service / module to instrument]
---

Use the `back-add-logs` skill to add structured logging via NestJS's built-in `Logger` to the target code — at the right places, at the right levels, without being noisy.

Target: $ARGUMENTS
