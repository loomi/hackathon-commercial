---
description: Write Vitest + RTL tests (and Playwright E2E if it's a flow) for the front-end
argument-hint: [component / hook / page / flow to test]
---

Use the `front-test` skill to add tests for the target — Vitest + React Testing Library for unit/component, MSW for HTTP mocking, axe for a11y, and Playwright for end-to-end when it's a user flow.

Target: $ARGUMENTS
