---
description: Scaffold a new NestJS feature module (controller, service, DTOs, Prisma model)
argument-hint: [resource name and brief requirements]
---

Use the `back-add-feature` skill to implement the requested feature, following this project's NestJS 11 + Prisma conventions and RESTful principles. If the work touches the database schema, also use `back-prisma-change` to produce the migration.

Request: $ARGUMENTS
