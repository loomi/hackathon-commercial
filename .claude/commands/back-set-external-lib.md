---
description: Wrap a third-party SDK / external service in a thin adapter module
argument-hint: [library or service to integrate]
---

Use the `back-set-external-lib` skill to integrate the requested third-party library or external service into this NestJS back-end with a thin adapter layer, so the rest of the codebase stays loosely coupled to the vendor.

Library / service: $ARGUMENTS
