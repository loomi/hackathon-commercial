---
description: Security review / hardening for Next.js routes, Server Actions, cookies, headers
argument-hint: [file or area to review]
---

Use the `front-security` skill to apply security best practices to the target Next.js (App Router) + TypeScript code — secrets/env hygiene, server-only modules, XSS / CSRF / open-redirect / SSRF prevention, secure cookies, CSP, route-handler & Server Action input validation, auth/session handling.

Target: $ARGUMENTS
