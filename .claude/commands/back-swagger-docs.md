---
description: Document a NestJS controller / DTOs with @nestjs/swagger decorators
argument-hint: [controller path or feature name]
---

Use the `back-swagger-docs` skill to fully document the target controller, route handlers, and DTOs with `@nestjs/swagger` decorators so the generated OpenAPI spec is accurate.

Target: $ARGUMENTS
