---
description: Write Jest unit tests (and Supertest e2e if relevant) for the back-end
argument-hint: [service / controller / module to test]
---

Use the `back-add-tests` skill to write or improve Jest unit tests (`*.spec.ts`) and, when relevant, Supertest e2e tests (`test/*.e2e-spec.ts`) for the target.

Target: $ARGUMENTS
