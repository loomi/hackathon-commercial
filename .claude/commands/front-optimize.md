---
description: Optimize Next.js code — Server vs Client split, dynamic imports, next/image, caching
argument-hint: [page / component to optimize]
---

Use the `front-optimize` skill to optimize the target for performance and bundle size — Server vs Client components, async data, Suspense + streaming, dynamic imports, `next/image`, `next/font`, route caching/revalidation, memoization, and Web Vitals.

Target: $ARGUMENTS
