---
description: Tighten TypeScript — kill any/unknown leaks, add discriminated unions, Zod inference
argument-hint: [file / module to tighten]
---

Use the `front-typescript-strict` skill to apply strict-mode TypeScript patterns — eliminating `any`/`unknown` leaks, discriminated unions, exhaustive checks, narrowing, branded types, `satisfies`, and schema-first typing with Zod inference where appropriate.

Target: $ARGUMENTS
